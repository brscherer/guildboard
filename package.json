{
  "name": "guildboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "start:api": "pnpm --filter @guildboard/api dev",
    "start:web": "pnpm --filter @guildboard/web dev",
    "cy:open": "cypress open --config-file tests/cypress.config.ts",
    "cy:run": "cypress run --config-file tests/cypress.config.ts",
    "stryker": "stryker run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.4",
  "dependencies": {
    "cypress": "^12.0.0",
    "turbo": "^2.5.6"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.1.1",
    "@stryker-mutator/vitest-runner": "^9.1.1",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2"
  }
}
